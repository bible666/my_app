sequenceDiagram
    participant user as Agent
    participant login as msa-nbsportal
    participant home as msa-agentauth
    participant news_list as msa-agentnewskm
    participant sea as msa-seaweedgw
    participant db

rect rgba(0, 0, 255, .1)
    Note over user,db: show news
    user->>login: input user/password
    login-->>login: Authen with Ocean PAD/LDAP
    login->>login: Create session login
    login->>home: create JWT
    home->>login:return JWT
    login->>user:JWT and show message
    user->>news_list: call server getNews with JWT Header
    news_list->>+db : send query for get news
    db-->>-news_list : return news data for agent
    news_list->>user : return news data for agent
    user->>+sea : download news document with JWT header
    sea->>-user : return file for agent
    user->>user : show news data
end
